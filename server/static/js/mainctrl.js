app.controller("mainctrl", ["$scope", "$rootScope", "$state", "HttpService",
    function($scope, $rootScope, $state, HttpService) {

        $scope.foo = {};

        $scope.bar = function() {

        }
    }
]);